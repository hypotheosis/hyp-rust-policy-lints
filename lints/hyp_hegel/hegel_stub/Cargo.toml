[package]
name = "hegeltest"
version = "0.14.0"
edition = "2021"
publish = false

[lib]
name = "hegel"

[dependencies]
hegeltest-macros = { path = "../hegel_stub_macros" }

# Not a releasable package. This stub deliberately mirrors the published
# `hegeltest` 0.14.x so the UI fixtures exercise production crate naming; a
# version bump would drift it away from the thing it imitates.
[package.metadata.release]
release = false
